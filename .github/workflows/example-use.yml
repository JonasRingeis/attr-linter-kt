on:
  push:
    branches: [ main ]
    paths-ignore:
      - README.md

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: JonasRingeis/attr-linter-kt/action.yml@main
        with:
          project-alias: 'ci'
